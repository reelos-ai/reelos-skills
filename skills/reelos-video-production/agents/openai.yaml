interface:
  display_name: "ReelOS 口播视频生产"
  short_description: "制作 TTS 同步并自动择效的 ReelOS 口播视频与 Slide Story"
  default_prompt: "Use $reelos-video-production to 先判断标准口播、SketchTalk 或 Slide Story 主模式，按真实 TTS timing 为每个 beat 生成并评分候选动效，自动选择语义最匹配的效果，再完成渲染、关键帧返修与验收。"
policy:
  allow_implicit_invocation: true
